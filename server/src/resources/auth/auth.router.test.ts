describe('users-router', () => {
  afterEach(() => {
    jest.clearAllMocks();
  });

  describe('get', () => {
    it('should successfully return user', async () => {
      expect(1).toEqual(1);
    });
  });
});
