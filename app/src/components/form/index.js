import React from 'react';
import PropTypes from 'prop-types';

DynamicForm.propTypes = {
  parameters: PropTypes.array,
};

function DynamicForm({ parameters }) {
  return (
    <div>
      COMING SOON
    </div>
  );
}

export default DynamicForm;
